<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chuiyan.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registe" />
        <activity android:name=".PhotoDetail" />
        <activity android:name=".Photo" />
        <activity android:name=".Login" />
        <activity android:name=".Logs" />
        <activity android:name=".LogsDetail" />
        <activity android:name=".Test" />
        <activity android:name=".Contacter" />
        <activity android:name=".AddContacter" />
        <activity android:name=".AddLogs"></activity>
    </application>

</manifest>